// putting the token and the base API url in here
// I make this file publicly available only because it is fake data

export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000',
  apiToken: 'tokenTest123',
};
